import os
from dotenv import load_dotenv

# Загрузка переменных окружения
load_dotenv()

class Config:
    # Telegram Bot Token
    TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")

    # Ограничения
    MAX_FILES = 10  # Максимальное количество файлов за одну загрузку
    MAX_FILE_SIZE = 50 * 1024 * 1024  # 50MB в байтах

    # Настройки экспорта
    EXPORT_THRESHOLD = 50  # Порог для переключения на Excel (50 участников)

    # Пути
    TEMP_DIR = "temp"  # Временная директория для обработки файлов

    # Сообщения бота
    WELCOME_MESSAGE = (
        "👋 Привет! Я бот для извлечения участников из истории чатов Telegram.\n\n"
        "📄 Загрузите файлы истории чата (до 10 файлов, до 50MB каждый).\n"
        "🔍 Я проанализирую их и верну список участников.\n\n"
        "💡 Поддерживаемые форматы: JSON и HTML (экспорт из Telegram)."
    )

    HELP_MESSAGE = (
        "📖 Как пользоваться ботом:\n\n"
        "1. Экспортируйте историю чата в Telegram (Настройки → Экспорт истории чата)\n"
        "2. Загрузите полученные файлы в этот чат (до 10 файлов за раз)\n"
        "3. Я обработаю файлы и верну:\n"
        "   - Список участников (если менее 50)\n"
        "   - Excel-файл (если 50 и более участников)\n\n"
        "⚠️ Ваши данные не сохраняются на сервере и обрабатываются только во время сессии."
    )

    ERROR_MESSAGE = "❌ Ошибка: {}"
    PROCESSING_MESSAGE = "⏳ Обработка файлов... Пожалуйста, подождите."
    RESULT_MESSAGE = "🎉 Готово! Вот результаты:"
    TOO_MANY_FILES = f"❌ Слишком много файлов. Максимум: {MAX_FILES} файлов."
    FILE_TOO_LARGE = f"❌ Файл слишком большой. Максимум: {MAX_FILE_SIZE // (1024*1024)}MB."
    UNSUPPORTED_FORMAT = "❌ Неподдерживаемый формат файла. Поддерживаются только JSON и HTML."

    # Структура Excel
    EXCEL_COLUMNS = [
        "Дата экспорта",
        "Username",
        "Имя и фамилия",
        "Описание",
        "Дата регистрации",
        "Наличие канала в профиле"
    ]